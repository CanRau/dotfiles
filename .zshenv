# checkout https://github.com/yonchu/dotfiles/blob/master/.zsh/.zshenv

# Setup ZDOTDIR.
# The directory to search for shell startup files (.zshrc, etc).
# If ZDOTDIR is unset, HOME is used instead.
#
ZDOTDIR=$HOME/.zsh